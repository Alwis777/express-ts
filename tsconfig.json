{
  "compilerOptions": {
    "rootDir": "src",          // Where your TypeScript source code lives
    "outDir": "dist",          // Where compiled JavaScript will be placed
    "module": "commonjs",      // JS module system (Node uses CommonJS)
    "target": "ES6",           // Compile TS into ES6-level JavaScript
    "strict": true,            // Turn on strict type-checking (safer code)
    "esModuleInterop": true    // Allows mixing default/import syntax easily
  },
  "include": ["src/**/*"],     // Compile everything inside src folder
  "exclude": ["node_modules", "dist"] // Skip these folders when compiling
}

//By separating src (your TypeScript code) and dist (compiled JS), 
// you keep the project clean. You never mix raw code and compiled code in the same place.